version: 2 
jobs:
  build:
    working_directory: ~
    docker:
        - image: circleci/node:10.13.0-browsers
    steps:
      - checkout
      - run: touch 1234.txt
      - run: ls .
      - run: node -v
      - run: cat /etc/issue
      - run: uname -a
      - run: pwd
      - run: npm install
      - run: ls node_modules
      - run: cat package.json
      - run: npm run build
      - run: npm test 
workflows:
  version: 2
  build-and-deploy:
    jobs:
      - build
